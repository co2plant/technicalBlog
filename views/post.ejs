<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> - <%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">기술 블로그</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/">홈</a>
                <a class="nav-link" href="/posts">글 목록</a>
                <a class="nav-link" href="/about">소개</a>
            </div>
        </div>
    </nav>
    
    <div class="container mt-4">
        <article class="post-detail">
            <header class="post-header mb-4">
                <h1 class="post-title"><%= post.title %></h1>
                <div class="post-meta text-muted">
                    <span class="post-date">작성일: <%= post.date %></span>
                    <% if (post.category) { %>
                        <span class="post-category ms-3">카테고리: <%= post.category %></span>
                    <% } %>
                </div>
            </header>
            
            <div class="post-content">
                <%- post.content %>
            </div>
            
            <footer class="post-footer mt-5">
                <hr>
                <div class="d-flex justify-content-between align-items-center">
                    <a href="/" class="btn btn-outline-primary">← 목록으로 돌아가기</a>
                    <div class="post-navigation">
                        <% if (post.prevPost) { %>
                            <a href="/posts/<%= post.prevPost.id %>" class="btn btn-outline-secondary me-2">← 이전 글</a>
                        <% } %>
                        <% if (post.nextPost) { %>
                            <a href="/posts/<%= post.nextPost.id %>" class="btn btn-outline-secondary">다음 글 →</a>
                        <% } %>
                    </div>
                </div>
            </footer>
        </article>
    </div>
    
    <footer class="bg-dark text-light text-center py-3 mt-5">
        <div class="container">
            <p class="mb-0">&copy; 2024 기술 블로그. All rights reserved.</p>
        </div>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
